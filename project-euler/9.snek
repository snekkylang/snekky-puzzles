let solve = func() {
    for let a in 1..1000 {
        for let b in 1..1000 - a {
            let c = 1000 - (a + b);

            if a * a + b * b == c * c {
                if a < b {
                    return a * b * c;
                }
            }
        }
    }
};

Sys.println(solve());